//JAVA SCRIPT IN <hea> or <body>

//you can place any number of scripts in an HTML docoment.
//Scripts can be placed in the  <body>, or in the <head> section of an HTML page,or in both.

//  JAVASCRIPT IN <HEAD>
// in this example, a js function is placed in the <head> section of an HTML page.
 // The f.n. is invoked (called) when a biutton is clicked.

function myfunction() {
    decodeURIComponent.getElementById("demo"). innerHTML = "paraggraph changed.";
}
